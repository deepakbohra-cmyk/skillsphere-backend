package com.skillsphere.skillsphere.user.enums;

public enum NotificationType {
    ASSESSMENT_REMINDER,
    ASSESSMENT_OVERDUE,
    ATTEMPT_LIMIT_BREACH,
    LEAD_NOTIFIED,
    NEW_CONTENT,
    BADGE_EARNED,
    CERTIFICATE_READY,
    ENROLLMENT_APPROVED
}
